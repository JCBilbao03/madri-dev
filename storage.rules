rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    function isAppCheckVerified() {
      return request.appCheck != null && request.appCheck.token.valid == true;
    }

    // Property media — readable by signed-in users; writes are admin/seed only for now.
    match /properties/{propertyId}/{fileName} {
      allow read: if request.auth != null;
      allow write: if false;
    }

    // Product photos for the inventory catalog demo.
    match /inventory/{itemId}/{fileName} {
      allow read: if true;
      allow write: if isAppCheckVerified()
        && request.resource.size < 5 * 1024 * 1024
        && request.resource.contentType.matches('image/.*');
      allow delete: if isAppCheckVerified();
    }

    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
