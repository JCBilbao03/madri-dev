/** Initial inbox rows and how many more to load per scroll batch. */
export const INBOX_BATCH_SIZE = 5;

/** Estimated row height for the virtualizer (px). */
export const INBOX_ROW_ESTIMATE_PX = 92;
